指针是一个对象
指针指的是地址。

利用地址的值，可以找到所需的变量单元。
即意为：地址的值直接指向存在电脑存储器中的另一个地方的值。

通过地址 能找到 所需要的变量单元，我们称呼这种方式为指向，（即地址指向了【所需要的】变量单元）
所以，地址就被形象化为指针。


存储器地址的值 直接指向 存在于该地址的对象的值  存在于存储器地址中的对象的值
就是说，存储器地址中有一个对象，这个对象有一个值。
而本身的存储器的值 指向了 改对象的值。



 
地址的值 指向 存在地址的对象的值 （将值存在于此地址对象）

值 指向 对象的值

值 指向 值