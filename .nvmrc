v16.15.0 // 主版本

v18.19.0 // 如果启动服务时设置 NODE_OPTIONS="--openssl-legacy-provider", 那么就能支持这个版本
// 为什么需要这么高的版本? 因为 @yomua/y-tlog 引入的依赖 chalk 使用了 node 高版本的 API
// => 即使把 chalk 内置到 @yomua/y-tlog 项目中, 使用的 node 高版本的 API 也是没变的.
// 注意: 我使用的 github pages 不支持 --openssl-legacy-provider, 因为 node 版本为 16.20.2
// => /opt/hostedtoolcache/node/16.20.2/x64/bin/node: --openssl-legacy-provider is not allowed in NODE_OPTIONS
// 所以 build 不允许使用这个设置